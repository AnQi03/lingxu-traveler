# 开发者模式设计方案

## 目标
按 `~` 键（或 `F12`）打开一个开发者面板，可以：
1. 修改灵石数量（下品/中品/上品）
2. 添加任意灵材到背包
3. 修改时间（前进/后退）
4. 重置/设置三道路径数值
5. 设置每日熔炼次数
6. 一键预置数据（方便测试特定场景）

## 方案一：纯代码生成 UI（推荐）

和 Market.gd 一样的模式——按 `~` 键创建 CanvasLayer，代码生成 Panel 作为面板。

### 触发方式
- 按 `F12` 或 `~`（反引号）打开/关闭开发者面板
- 只在 debug 构建中启用（或通过 `--dev-mode` 参数）

### 输入映射
```
DEV_CONSOLE = KEY_F12 或 KEY_GRAVE（反引号）
```

### UI 结构
```
CanvasLayer (layer=10, 最高层)
└── Panel (800x600, 居中)
    ├── 标题栏: "🧪 开发者控制台"
    ├── VBoxContainer
    │   ├── 灵石区块
    │   │   ├── HBox: 下品 [输入框] [设置按钮]
    │   │   ├── HBox: 中品 [输入框] [设置按钮]
    │   │   └── HBox: 上品 [输入框] [设置按钮]
    │   ├── 时间区块
    │   │   └── HBox: 天数 [输入框] 时辰 [HBox:时/分] [设置]
    │   ├── 灵材区块
    │   │   └── HBox: 下拉选择灵材 + 数量 [添加]
    │   ├── 三道区块
    │   │   └── HBox: 商道/天道/人心 [输入框] [设置]
    │   └── 快捷预设
    │       └── HBox: [开局默认] [测试摆摊] [测试熔炉] [测试战斗]
    └── 关闭按钮
```

### 文件结构
- 新增 `scripts/DevConsole.gd` — 开发者面板逻辑
- 在 `Main.gd` 中添加 `KEY_F12` 的映射和调用

### 菜单中隐藏
- `PROJECT_SETTINGS` 中加一个 `debug/dev_mode` 开关
- 或者直接用 `OS.is_debug_build()` 判断

## 第二天实现步骤

1. 创建 `scripts/DevConsole.gd`
2. 实现 UI 布局（代码生成）
3. 实现各功能模块（灵石/灵材/时间/三道）
4. 在 Main.gd 注册 F12 或 `~` 键
5. 测试
